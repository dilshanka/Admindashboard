/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.pexels.com",
      // Add other remote domains here if needed
    ],
  },
};

module.exports = nextConfig;
